# setup.py
from setuptools import setup, find_packages

setup(
    name='eventing_subsystem',
    version='1.0.0',
    author='Travis Avery',
    python_requires='>=3.7',
    packages=find_packages()
)